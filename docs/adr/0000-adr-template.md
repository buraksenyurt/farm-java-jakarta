# ADR-0000: `<Short title of the decision>`

- **Status:** Proposed | Accepted | Rejected | Superseded by ADR-XXXX
- **Date:** YYYY-MM-DD
- **Deciders:** names

## Context

What problem are we solving? Under which constraints are we deciding?
State the forces at play: technical, organisational, and the ones that will still be true in two years.

## Options considered

1. **`<Option A>`** — pros / cons
2. **`<Option B>`** — pros / cons

## Decision

One sentence, imperative mood.

## Consequences

- Positive: ...
- Negative / the price we pay: ...

## How this is enforced

| Rule | Test |
| --- | --- |
| `<the rule in one sentence>` | `<TestClass>.<fieldName>` |

> An ADR without this section is a wish, not a decision. If a rule cannot be expressed as an ArchUnit rule, say so explicitly and name the manual control that replaces it (code review checklist, PR template item, Sonar rule).
